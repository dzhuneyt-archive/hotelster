{
    "private": true,
    "scripts": {
        "dev": "npm run installDeps && npm run seedBackend && npm run frontendTests && npm run backendAndFrontendLiteServer",
        "installDeps": "npx concurrently 'npm run installDependenciesBackend' 'npm run installDependenciesFrontend'",
        "installDependenciesBackend": "php composer.phar install",
        "installDependenciesFrontend": "cd frontend && npm install",
        "seedBackend": "php artisan migrate:fresh --seed",
        "backendLiteServer": "npx concurrently 'npm run tailLogs' 'php artisan serve --host=0.0.0.0'",
        "frontendLiteServer": "cd frontend && npm run start",
        "backendAndFrontendLiteServer": "npx concurrently 'npm run backendLiteServer' 'npm run frontendLiteServer'",
        "frontendTests": "cd frontend && npm run test:cli",
        "tailLogs": "tail -n0 -f ./storage/logs/*.log"
    },
    "devDependencies": {
        "axios": "^0.18",
        "bootstrap": "^4.1.0",
        "cross-env": "^5.1",
        "jquery": "^3.5",
        "laravel-mix": "^4.0.7",
        "lodash": "^4.17.5",
        "popper.js": "^1.12",
        "resolve-url-loader": "^2.3.1",
        "sass": "^1.15.2",
        "sass-loader": "^7.1.0",
        "vue": "^2.5.17",
        "concurrently": "^4.1.1"
    }
}
